export enum VehicleType {
    Car = "Car",
    Bike = "Bike",
    Heavy = "Heavy"
}

export enum SpotType {
    Small = "Small",
    Medium = "Medium",
    Large = "Large"
}

export enum PaymentStatus {
  Completed = 'Completed',
  Failed = 'Failed',
  Pending = 'Pending',
}

export enum PaymentMethod {
  Card = 'Card',
  Cash = 'Cash',
  UPI = 'UPI',
} 